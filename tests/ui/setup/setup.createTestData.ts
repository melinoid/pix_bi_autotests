import { test as setup } from '../../../utils/fixtures';

setup('Генирируем тестовые данные', async () => {
  // Сущности со случайными атрибутами для смоков
  // writeData('user_crud', await UsersTD.createUser()); // in 6.1.1.
  // writeData('group_crud', await GroupsTD.createGroup()); // in 6.1.4.
  // writeData('license_rule_crud', await LicenseRulesTD.createRule()); // in 6.2.1.
  // writeData('ldap_connector_crud', await LDAPConnectorsTD.createLDAPConnector()); // in 6.1.7.
  // writeData('directory_crud', await DirectoriesTD.createDirectory()); // in 2.1.6.
  // writeData('application_crud', await ApplicationTD.createApplication()); // in 2.1.1.
});
